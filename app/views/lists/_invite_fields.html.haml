%tr.nested-fields
  - if f.object.new_record?
    %td= f.text_field :name
    %td= f.text_field :email
  - else
    %td= f.object.name
    %td= f.object.email
  %td.controls= link_to_remove_association "remove", f
